---
title: OpenClaw - セルフホスト型AIスマートアシスタントプラットフォーム
description: OpenClaw チュートリアル — OpenClaw のインストール、TokenFactory との連携、セルフホスト型AIアシスタントの迅速な構築。オープンソースプロジェクトで、Telegram、Discord、WhatsAppなど複数のチャネル統合をサポート。
---

<Callout type="info" title="プロジェクト紹介">
  OpenClaw は、オープンソースのセルフホスト型パーソナルAIアシスタントプラットフォームで、メッセージングアプリを自身のハードウェアで動作するAIエージェントに接続します。開発者や上級ユーザー向けに設計されており、データ制御権を手放すことなく、自律的なAIアシスタントを所有できます。

  - 公式サイト：[https://openclaw.ai](https://openclaw.ai)
  - プロジェクトドキュメント：[https://docs.openclaw.ai](https://docs.openclaw.ai)
  - GitHub：[https://github.com/openclaw/openclaw](https://github.com/openclaw/openclaw)
</Callout>

OpenClaw は完全にオープンソースであり、[OpenClaw のGitHubリポジトリ](https://github.com/openclaw/openclaw)でソースコードを閲覧したり、Issueを提出したり、貢献したりできます。このチュートリアルでは、OpenClaw のインストール、設定、およびToken Factoryとの連携の完全な手順を説明します。

## 🌟 主要機能

### マルチチャネル統合

- **マルチチャネル統合**：Telegram、Discord、WhatsApp、iMessageなど、複数のメッセージチャネルをサポートし、プラグインを通じてさらに多くのプラットフォームに拡張可能
- **単一ゲートウェイ**：単一のGatewayプロセスを通じてすべてのチャネルを一元管理
- **音声サポート**：macOS/iOS/Androidの音声インタラクションをサポート
- **Canvasインターフェース**：インタラクティブなCanvasインターフェースをレンダリング可能

### セルフホスティングとデータセキュリティ

- **完全セルフホスティング**：自身のマシンまたはサーバー上で動作
- **オープンソースの透明性**：MITオープンソースライセンス、コードは完全に透明
- **データローカライゼーション**：コンテキストとスキルはクラウドではなく、ローカルコンピューターに保存

### インテリジェントエージェント機能

- **継続実行**：バックグラウンドでの常駐実行をサポートし、永続的な記憶を持つ
- **スケジュールタスク**：cronによる定期タスクをサポート
- **セッション分離**：エージェント/ワークスペース/送信者ごとにセッションを分離
- **マルチエージェントルーティング**：複数のエージェントが連携して動作することをサポート
- **ツール呼び出し**：ツール呼び出しとコード実行をネイティブにサポート

## 📦 連携前の準備

<Callout type="info" title="準備情報">
  - Node.js 22 以降
  - 利用可能なToken Factoryアドレス（通常`/v1`で終わる）
  - 利用可能なToken FactoryのAPIキー
</Callout>

Token Factoryとの連携を開始する前に、OpenClawの公式推奨手順に従ってGatewayとControl UIを起動しておくことをお勧めします。これにより、後で問題が発生した場合に、OpenClaw自体が起動していないのか、モデルプロバイダーの設定が間違っているのかを区別しやすくなります。

### 1. OpenClawのインストール（macOS/Linux）

```bash
curl -fsSL https://openclaw.ai/install.sh | bash
```

その他のインストール方法については、OpenClawの公式ドキュメント：[Getting Started](https://docs.openclaw.ai/start/getting-started)を参照してください。

### 2. オンボーディングウィザードの実行

```bash
openclaw onboard --install-daemon
```

このウィザードは、基本認証、Gateway設定、およびオプションのチャネル初期化を完了します。ここでの目標は、まずOpenClawを起動し、その後デフォルトモデルをToken Factoryに切り替えることです。

### 3. GatewayとControl UIの確認

```bash
openclaw gateway status
```

```bash
openclaw dashboard
```

ブラウザでControl UIが開ければ、OpenClawの基本動作は正常です。この段階では、Telegram、Discord、飞书などのメッセージチャネルを先に設定する必要はありません。

### 4. 設定ファイルの特定

OpenClawの設定ファイルは通常`~/.openclaw/openclaw.json`にあり、オンボーディングウィザードで生成されたものを基にさらに変更できます。

<Callout type="tip" title="パス関連の環境変数">
  OpenClawを専用サービスアカウントで実行する場合、または設定/ステータスディレクトリをカスタマイズしたい場合は、以下を使用できます：

  - `OPENCLAW_HOME`
  - `OPENCLAW_STATE_DIR`
  - `OPENCLAW_CONFIG_PATH`

  詳細については、公式の環境変数ドキュメント：[Environment Variables](https://docs.openclaw.ai/help/environment)を参照してください。
</Callout>

## 🚀 Token Factoryをモデルプロバイダーとして使用する

OpenClawは、`models.providers`を通じてカスタムまたはOpenAI互換インターフェースのモデルゲートウェイへの連携をサポートしています。Token Factoryの場合、最も一般的な方法は、それをカスタムプロバイダーとして設定に追加し、デフォルトモデルを`tokenfactory/モデルID`に指定することです。

### 連携の考え方

1.  `models.providers`の下で`tokenfactory`プロバイダーを宣言する
2.  `baseUrl`をToken Factoryアドレスに指定し、`/v1`が含まれていることを確認する
3.  `api`を`openai-completions`に設定する
4.  `models`にOpenClawが使用したいモデルIDをリストする
5.  `agents.defaults.model.primary`でデフォルトモデルを`tokenfactory/モデルID`に切り替える

### 推奨される方法：環境変数でキーを保存する

まず、現在のシェル、サービス環境、またはOpenClawが読み取れる`.env`ファイルでToken Factoryキーを提供します：

```bash
export TOKENFACTORY_API_KEY="sk-your-tokenfactory-key"
```

次に、`openclaw.json`に以下のスニペットを追加または変更します：

```json5
{
  models: {
    mode: "merge",
    providers: {
      tokenfactory: {
        baseUrl: "https://<your-tokenfactory-domain>/v1",
        apiKey: "${TOKENFACTORY_API_KEY}",
        api: "openai-completions",
        models: [
          { id: "gemini-2.5-flash", name: "Gemini 2.5 Flash" },
          { id: "kimi-k2.5", name: "Kimi K2.5" },
        ],
      },
    },
  },

  agents: {
    defaults: {
      model: {
        primary: "tokenfactory/gemini-2.5-flash",
        fallbacks: ["tokenfactory/kimi-k2.5"],
      },
      models: {
        "tokenfactory/gemini-2.5-flash": { alias: "flash" },
        "tokenfactory/kimi-k2.5": { alias: "kimi" },
      },
    },
  },
}
```

これはそのままコピーする必要がある完全な設定ではなく、Token Factoryとの連携において最も重要な部分です。`provider`、モデルID、およびデフォルトモデルの参照が正しく対応していれば、OpenClawはToken Factoryを通じて公開されているモデルリソースを呼び出すことができます。

### 主要な設定の説明

| 設定項目 | 説明 |
| --- | --- |
| `models.mode` | `merge`に設定することをお勧めします。OpenClawの組み込みプロバイダーを保持しつつ、`tokenfactory`を追加します。 |
| `models.providers.tokenfactory.baseUrl` | あなたのToken Factoryアドレス。通常`/v1`を含める必要があります。 |
| `models.providers.tokenfactory.apiKey` | Token Factoryキー。`${TOKENFACTORY_API_KEY}`を通じて注入することを推奨します。 |
| `models.providers.tokenfactory.api` | Token FactoryのようなOpenAI互換ゲートウェイの場合、`openai-completions`を使用します。 |
| `models.providers.tokenfactory.models` | ここにリストされているモデルIDは、Token Factoryが実際に公開しているモデル名と一致している必要があります。 |
| `agents.defaults.model.primary` | デフォルトのプライマリモデル。形式は`provider/model-id`である必要があります。 |
| `agents.defaults.model.fallbacks` | フォールバックモデルのリスト。プライマリモデルが失敗した場合に自動的に切り替わります。 |
| `agents.defaults.models` | オプション。モデルにエイリアスを付けるために使用され、UIやセッションでの参照を容易にします。 |

### 連携成功の確認

設定が完了したら、Control UIに戻るか、再度開きます：

```bash
openclaw dashboard
```

OpenClawで正常に会話を開始でき、デフォルトモデルが`tokenfactory/...`になっている場合、連携は成功です。また、以下を使用することもできます：

```bash
openclaw models list
```

`tokenfactory/`プレフィックスのモデルが選択可能なリストに表示されていることを確認します。

### よくある問題

-   `baseUrl`に`/v1`が含まれていない：これは最も一般的な連携エラーの1つです。
-   モデルIDの入力ミス：`primary`と`fallbacks`は`models.providers.tokenfactory.models`内の`id`と一致している必要があります。
-   キーが現在のターミナルでのみ有効：Gatewayがバックグラウンドサービスとして実行されている場合、サービスプロセスも`TOKENFACTORY_API_KEY`を読み取れることを確認してください。
-   フォアグラウンドでのトラブルシューティング：公式のフォアグラウンド実行方法`openclaw gateway --port 18789`を使用して、ログとエラーを確認できます。